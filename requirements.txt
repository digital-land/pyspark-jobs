# Production dependencies for PySpark Jobs for EMR Serverless
# This file excludes packages that are pre-installed in EMR Serverless:
# - PySpark (pre-installed)
# - boto3 (pre-installed, use native AWS SDK)
# - botocore (pre-installed, use native AWS SDK)

# Database connectivity (pure Python - no binary dependencies)
pg8000>=1.30.0
# NOTE: DO NOT use psycopg2-binary>=2.9.0 - contains platform-specific compiled C extensions
# that cause "ModuleNotFoundError: No module named 'psycopg2._psycopg'" in EMR Serverless.
# See docs/DATABASE_CONNECTIVITY.md for detailed explanation.

# Configuration and data handling
PyYAML>=6.0

# CLI framework
click>=8.0.0

# HTTP requests
requests>=2.28.0

# Cloud path handling (consistent s3:// and local path joining)
cloudpathlib>=0.18.0

# Data validation
pydantic>=2.0.0

# Delta Lake Python bindings (delta-spark) are intentionally omitted here.
# delta-spark depends on pyspark, which conflicts with the manylinux cross-platform
# install used in bin/build_aws_package.sh. It is installed separately with --no-deps
# in that script. For local development, install it manually: pip install delta-spark

# Python standard library extensions
typing-extensions>=4.0.0

# Package metadata
setuptools>=65.0.0